#include "database.h"
#include "hashmap.h"

#include <stdio.h>

int main(int argc, char *argv[]) {
    input_parsing(argc, argv);
    //int is_found = 0;
    //printf("%s %d\n", find_dat_str("a.dat", "w", &is_found), is_found);
    //printf("%d", is_found);
    return 0;
}
